codex "We are building a new project called memos-rs in the current repository. Goal: reimplement the Memos backend in Rust as a self-hostable single-binary service for Debian VPS and Arch Linux, with GitHub Actions builds and releases. Important context: upstream Memos is already a single Go binary with a React frontend and supports SQLite, MySQL, and PostgreSQL; we are NOT rewriting the whole product at once; we are building a Rust backend foundation first; keep frontend out of scope unless needed later; SQLite is the first database target. Technical stack: Rust stable, axum, tokio, serde, sqlx with SQLite first, clap, tracing, tower-http, argon2, jsonwebtoken or equivalent. Constraints: build incrementally, keep architecture clean, avoid unnecessary abstraction, avoid unwrap/expect in runtime code, add tests where practical, every milestone must end in a compiling and runnable state. Implement now: create src/main.rs, src/cli.rs, src/config.rs, src/server.rs, src/app.rs, src/error.rs, src/state.rs, migrations/, .github/workflows/ci.yml, config.example.toml, README.md, packaging/memos-rs.service. Build a minimal runnable service with serve subcommand, config loading from file and env, axum server, GET /healthz returning JSON, structured logging, graceful shutdown, and CI running fmt, clippy, test, and release build. Output a short plan, file tree, full implementation, exact local test commands, and next steps. Do not stop at planning. Start implementing."