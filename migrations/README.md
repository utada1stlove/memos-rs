# Migrations

The service applies SQLx migrations automatically on startup.

Phase 2 adds the initial SQLite schema for:

- `users`
- `memos`

Manual SQLx CLI commands are documented in the README for local inspection or rollback workflows.
